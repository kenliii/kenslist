<!DOCTYPE html>

<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>kenslist events || <%= events.title %></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="/css/style.css" rel="stylesheet" type="text/css">
        <%- include ('../partials/head.ejs') %>
    </head>
    <body>
      <nav>
        <div class="navbar">
          <a href='/' class ='ourlistpadding'>OURLIST</a>          <ul id="nav-mobile" class="right hide-on-med-and-down">
            <li><a href="/events">Go Back</a></li>
            <li><a href="/events/new">Create a New Listing!</a></li>
            <li><a href="/events">View all events listings</a></li>
        </ul>
        </div>
      </nav>
		<main>

			<section class = 'showcard'>
        <span class ='craigslist'>listing details:</span>
          <br><br>
          <span class='headingtitle'>'<%= events.title %> '</span>
  <br>
  <br>  
                  <div>
                  <div class="row">
                        <div class="card">
                          <div class="card-image">
                          </div>
                          <div class="card-content">
              <ul>
                      <li>Title: <%= events.title %></li>
                      <li>Price: $<%= events.price %></li>
                      <li>Description: <%= events.description %></li>
              </ul>
              </div>
              <div class="card-action">                 
                  <a class = 'btn waves-effect waves-light' id='clbutton' href="/events/<%=events._id %>/edit">Edit</a>
                  <form class ='btn waves-effect waves-light' id='clbutton' action="/events/<%= events._id %>?_method=DELETE" method="POST"> <input type="submit" style="color: white;" value="Delete" />
                  </form> 
              </div>
            </div>
          </div>
        </div>
      </div>     
          <div>
          </div>
          </div>
        </section>
        <form class="comments" 
        action="/events/<%= events._id %>/comments" method="POST">
        <!-- <label>Comments</label><br> -->   
        <textarea name="content" id ='commentbox' placeholder='add a comment here!'></textarea>
        <br>
        <input type="submit" class='btn waves-effect waves-light' id='clbutton' value="Add Comment">
      </form>
      <br><br>
      <% if (events.comments.length) { %>
      <body>
        <div>
          <p class="collection-header"style ='text-align:center'><span class='craigslist'>listing comments</span></p>
        <% events.comments.forEach(function(r) { %>
            <ul class="collection with-header">
                <li class ='collection-item'><div><span style="color: grey;"><%= r.createdAt.toLocaleDateString() %> </span>
                  <br><br>
                <%= r.content %>
              </div></li> 
                </div>
      </ul>
      <% }); %>
      <% } else { %>
          <h5>No Comments Yet</h5>
          <% } %> 
      </main>
    <footer class="page-footer"id ='navbar'>
      <span id='footerpadding'>
    Lead Developers: Jack Glazer & Nat Z
      </span>
</footer>

    </body>
</html>